{
  "name": "epsilon-voice-interface",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "main": "dist-electron/main/main.js",
  "scripts": {
    "dev": "vite",
    "build": "tsc -p tsconfig.main.json && tsc -p tsconfig.renderer.json && vite build",
    "test": "vitest run",
    "test:ui": "vitest --ui",
    "smoke": "node dist-electron/smoke/smoke.js"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "@vitest/ui": "^4.0.15",
    "electron": "^39.2.6",
    "typescript": "~5.9.3",
    "vite": "^7.3.1",
    "vitest": "^4.0.15"
  }
}
